rgb.led.count 6
encoder.line 5
# Grove chainable RGB LED data line. Default 24 (BCM 24 clock + BCM 25 data).
# Do NOT use 26: the data pin (line+1 = BCM 27) is the LoRa HAT's M1 mode pin,
# which collides ("Device or resource busy") when a LoRa HAT is also seated.
led.line 24
